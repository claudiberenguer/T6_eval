# Práctica Evaluación módulo Larga Language Models

Se proporcionan 3 jupyter notebook:
- v1 contiene un RAG báico
- v2 con memoria usando LangGraph.
- v3 es el DEFINITIVO con memoria y tool calling usando LangGraph. Base de datos ChromaDB. Se ha implmentado una fucnión para control de tokens en prompt pepero falla por el modelo usado no ser soportado por tiktoken. Ya visto el momento en que se está, de deja así

En los notebook se encuentran las explicaciones detalladas
